{% macro renderPanel(entry, hasArrows, panelType) %}
    {% if panelType is not defined or panelType is null %}
        {% set panelType = entry.type %}
    {% endif %}
    {% if entry.type != "buttons" %}
        {% include "panels/" ~ panelType %}
    {% endif %}
{% endmacro %}